name: fitness_pro
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter



  cupertino_icons: ^0.1.3
  camera: ^0.5.8+11
  tflite: ^1.1.1
  font_awesome_flutter: ^8.10.0
  line_awesome_flutter: ^1.0.1
  pedometer:
    git:
      url: git://github.com/feelfreelinux/flutter-plugins.git
      path: packages/pedometer
  shared_preferences: ^0.5.7+3
  google_fonts: ^1.1.0
  jiffy: ^3.0.1
  hive: ^1.4.1+1
  hive_flutter: ^0.3.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:


  uses-material-design: true

  assets:
   - assets/posenet_mv1_075_float_from_checkpoints.tflite
   - images/

